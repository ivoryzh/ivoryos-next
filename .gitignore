# Python
__pycache__/
.pytest_cache/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
ENV/

# Node
node_modules/
.pnp
.pnp.js
coverage/
.next/
out/
build/
.DS_Store
*.pem
# AWS IoT / MQTT certs (per-device and daemon private keys + CA chain) — *.pem above didn't
# actually cover these: private keys are .key, and the downloaded root CA is .crt.
*.key
*.crt
cloud_frontend/certs/
edge_server/ivoryos_edge/.certs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Local SQLite
*.db
*.sqlite3

# Auto-dumped on every edge server startup (server.py's startup_event) for local inspection —
# pure regenerated output, not source; was previously tracked and just churned as diff noise.
# NOT cloud_frontend/public/ivoryos_schema.json — that one IS real, fetched at runtime by
# cloud_frontend/src/app/edge-sequence/page.tsx as a static demo fixture, so it stays tracked.
/ivoryos_schema.json
/example/ivoryos_schema.json

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
